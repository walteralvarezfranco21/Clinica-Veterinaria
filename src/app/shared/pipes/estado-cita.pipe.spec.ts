import { EstadoCitaPipe } from './estado-cita.pipe';

describe('EstadoCitaPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoCitaPipe();
    expect(pipe).toBeTruthy();
  });
});
